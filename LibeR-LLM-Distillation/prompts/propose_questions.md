You are creating training material for a compact pharmacometrics assistant.

Use only the supplied source passage. Propose questions that a LibeR user could
realistically ask and that the passage can answer. Prefer questions requiring
reasoning, troubleshooting, interpretation, or correct code over trivial recall.
Do not introduce facts that are absent from the passage.

Return JSON matching the requested schema. For each proposal:

- make the question self-contained;
- select one supplied category;
- quote one or more short, exact evidence spans from the passage;
- include a difficulty of `basic`, `intermediate`, or `advanced`;
- avoid personal data, credentials, machine-specific paths and generated names.
