% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scheduler.R
\name{ls_grid_engine_executor}
\alias{ls_grid_engine_executor}
\title{Configure a Grid Engine worker executor}
\usage{
ls_grid_engine_executor(
  queue = "",
  project = "",
  parallel_environment = "smp",
  memory_resource = "mem",
  runtime_resource = "h_rt",
  tmpfs_resource = "tmpfs",
  memory_per_core = TRUE,
  tmpfs_mb = NULL,
  max_cores_per_job = max(1L, parallel::detectCores(logical = TRUE), na.rm = TRUE),
  job_name_prefix = "liber",
  prologue = character(),
  extra_submit_args = character(),
  submit = unname(Sys.which("qsub")),
  query = unname(Sys.which("qstat")),
  accounting = unname(Sys.which("qacct")),
  cancel = unname(Sys.which("qdel")),
  rscript = file.path(R.home("bin"), "Rscript"),
  shell = "/bin/bash",
  storage_key_file = Sys.getenv("LIBERTIES_SCHEDULER_STORAGE_KEY_FILE", unset = ""),
  accounting_grace_seconds = 120,
  command_timeout = 30
)
}
\arguments{
\item{queue, project}{Optional Grid Engine queue and project.}

\item{parallel_environment}{Parallel environment used for multi-core jobs.}

\item{memory_resource, runtime_resource, tmpfs_resource}{Site resource names.}

\item{memory_per_core}{Interpret the LibeRties memory limit as a whole-job
ceiling and divide it across requested Grid Engine slots.}

\item{tmpfs_mb}{Optional temporary-filesystem request in MB.}

\item{max_cores_per_job}{Maximum `n_cores` accepted for one job.}

\item{job_name_prefix}{Prefix used for scheduler-visible job names.}

\item{prologue}{Trusted shell statements run before the R worker.}

\item{extra_submit_args}{Additional trusted `qsub` arguments.}

\item{submit, query, accounting, cancel}{Grid Engine command paths or names.}

\item{rscript, shell}{Compute-node Rscript and shell paths.}

\item{storage_key_file}{Optional shared, mode-0600 file containing the
hexadecimal LibeRties storage key.}

\item{accounting_grace_seconds}{Time allowed for scheduler accounting to
publish a completed job before an absent job is considered lost.}

\item{command_timeout}{Scheduler command timeout in seconds.}
}
\value{
A serializable `liberties_grid_engine_executor` specification.
}
\description{
The adapter supports Sun/Oracle/Univa/Open Grid Engine command-line
interfaces and UCL Myriad's `smp`, `h_rt`, `mem`, and optional `tmpfs`
resources. Memory is requested per core by default, matching Myriad.
}
